apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-comentarios
  namespace: petdate
  labels: { app: ms-comentarios, tier: app }
spec:
  replicas: 2
  selector: { matchLabels: { app: ms-comentarios } }
  template:
    metadata: { labels: { app: ms-comentarios, tier: app } }
    spec:
      nodeSelector: { tier: app }
      containers:
        - name: ms-comentarios
          image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/repo_petdate_ms-comentarios:${IMAGE_TAG}
          ports: [{ containerPort: 8085 }]
          envFrom:
            - configMapRef: { name: petdate-config }
          env:
            - name: SERVER_PORT
              value: "8085"
            - name: SPRING_DATA_MONGODB_URI
              valueFrom: { secretKeyRef: { name: petdate-secrets, key: SPRING_DATA_MONGODB_URI } }
            - name: MAIL_USERNAME
              valueFrom: { secretKeyRef: { name: petdate-secrets, key: MAIL_USERNAME } }
            - name: MAIL_PASSWORD
              valueFrom: { secretKeyRef: { name: petdate-secrets, key: MAIL_PASSWORD } }
          resources:
            requests: { cpu: "150m", memory: "384Mi" }
            limits: { cpu: "500m", memory: "768Mi" }
          readinessProbe: { httpGet: { path: /actuator/health, port: 8085 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe: { httpGet: { path: /actuator/health, port: 8085 }, initialDelaySeconds: 45, periodSeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: ms-comentarios, namespace: petdate }
spec:
  type: ClusterIP
  selector: { app: ms-comentarios }
  ports: [{ protocol: TCP, port: 8085, targetPort: 8085 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: ms-comentarios-hpa, namespace: petdate }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: ms-comentarios }
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 50 } }
